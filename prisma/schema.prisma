generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PRISMA_DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Job {
  id          String   @id @default(cuid())
  title       String
  slug        String   @unique
  description String
  company     String
  location    String?
  type        String
  salary      String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  requirements String[]
  email       String
  postedAt    DateTime @default(now()) // Added to match lib/jobs.ts
}